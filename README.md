Follow these steps to run the project on an Android or iOS

Install dependencies: Make sure you use npm not yarn.

npm install

Install ios app:

npx react-native run-ios
Install android app:

npx react-native run-android
Run unit tests:

npm run test
